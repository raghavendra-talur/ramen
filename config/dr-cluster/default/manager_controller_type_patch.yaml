# This patch sets the RAMEN_CONTROLLER_TYPE environment variable for the dr-cluster operator
apiVersion: apps/v1
kind: Deployment
metadata:
  name: operator
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
        - name: RAMEN_CONTROLLER_TYPE
          value: dr-cluster
